# Open Questions

## Product Questions

- `Which sorting algorithms should be included in the first classroom demo?`
- `Should users be able to define an expected output, or should the site compute it automatically?`
- `Which explanations should be bilingual for the target course audience?`

## Technical Questions

- `Should the first visualizer use HTML/CSS bars or SVG for array elements?`
- `How should traces represent duplicate values and stable ordering?`
- `Which browser and mobile viewport sizes must be verified before classroom use?`

## Harness Questions

- Which checks can run before product code exists?
- Which checks require fixtures?
- Which checks require UI or runtime behavior?
